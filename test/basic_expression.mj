class Parent {
    int x = 1;
    Parent(int x) {
        this.x = x;
    }
    int foo(int x) {
        return 0;
    }
}

class Child extends Parent {
    int y = 2;
    Child(int y) {
        this.y = y;
    }
    int foo(char x) {
        return 10;
    }
}

int main() {
    var child = new Child(1);
    child.foo(20);
    return 0;
}